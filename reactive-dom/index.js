/**
 * Extensions to HTML elements that utilizes reactive values from @easedotjs/reactive
 * and exposes them to components if using @easedotjs/components.
 */

/**
 * Given an element, makes it and optionally its children reactive.
 * @param {*} element The element to make reactive
 * @param {*} deep If true, makes children reactive as well
 */
function makeReactive(element, deep = true) {}